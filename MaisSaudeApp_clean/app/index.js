import React from 'react';
import { View } from 'react-native';

// Placeholder to avoid expo-router rendering `HomeScreen` outside NavigationContainer.
// The real navigation is provided by App.js.
export default function _AppPlaceholder() {
	return <View />;
}
